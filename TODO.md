# Snake — To-Do List

| Status | Item | Notes |
|--------|------|-------|
| ✅ Done | v1.70 Advanced mode rework | Coin economy + 3-button ability bar replaced with a Vampire-Survivors-style XP bar + level-up system (`#levelup-overlay`, 2-3 random ability picks per level). Sprint/Dash moved from classic (removed there) into the advanced ability pool. 8 abilities total: Sprint, Dash, Tongue, Slow Time, Sidekick, Armor, Magnet, Weighted Ring — see `ABILITY_CFG` in client.js. |
| 🧪 Test | v1.73 Ability batch — 11 new abilities + slot/cap rules | Untested end-to-end on-device (Playwright-verified only). Hold/Tap are now single-occupancy slots and there's a run-wide cap (`MAX_ABILITY_SLOTS = 6`) on distinct abilities, VS-style — see `project_ability_slot_rules` memory. New: Reverse Thrust, Nimble Tail, Rattle, Phase Tail (tap-slot alternatives to Dash), Pit Sense (hold-slot alternative to Sprint), Echo, Big Fish, Keen Scent (auto), Chain Reaction, Efficient Digestion, Iron Scales (passive). Pool is now 19 abilities total. Remaining un-implemented ideas (Molt, Segment Merge, Wall Wrap, Burrows, Autopilot, Serpentine Spine, Scale Trail, Serpent's Luck) are queued for a future batch — Serpentine Spine specifically needs a real input-queue refactor (multi-turn-per-tick), deferred deliberately. |
| ⬜ To Do | Ability art | All 19 abilities (`ABILITY_CFG` in client.js) currently have no dedicated art — level-up cards are text-only. Need an icon per ability (and ideally per level/tier) for the level-up cards, plus any in-world indicator icons (e.g. the Dash ready-ring is a plain canvas-drawn circle right now). |
| ⬜ To Do | Snake visual changes per ability + level | Snake's on-canvas appearance (color/pattern/accessories) should change based on which abilities are active and their levels — e.g. a visible ring/collar for Weighted Ring, armor plating for Armor, etc. Depends on the ability art above existing first. |
| ⬜ To Do | Coyote time experiments | Try: (1) wall coyote — 1 tick of grace if snake just clipped the wall, reverse direction instead of dying; (2) self-collision grace — currently tail tip is already excluded (`slice(0,-1)`), experiment with excluding 2 tail segments; (3) input buffer window — queue up to 2 direction inputs instead of 1 so fast double-turns feel more responsive. |
| ⬜ To Do | Snake trail system | Snake leaves a visible dirt/worn-grass trail on the bg where it has traveled. Track visited cells in a Map; overlay a semi-transparent dirt color per cell each draw frame. No second image needed — just a colored rect overlay between the bg and the snake. Optional: trail fades slowly over time so only recent path is dark. |
| ⬜ To Do | Food sprite variants | 3–5 fruit sprites (apple, strawberry, watermelon, cherry, grape) randomly assigned each spawn. 256×256, magenta BG for removal. |
| ✅ Done | v1.13 visual juice | Grass BG, bobbing food with shadow, debris particles, snake shadow, screen shake on eat, score pop (+1). |
| ✅ Done | v1.09 Advanced mode | Coins, shop block, 3 abilities (Tongue / Slow Time / Sidekick), ability bar UI, shop overlay. |
| ✅ Done | Per-mode/difficulty score tracking | Scores stored as `"${mode}-${difficulty}"` key; leaderboard filters by active mode+diff. |
| ✅ Done | Scale texture | Snake body uses `snake_scale.png` canvas pattern, anchored per-segment with setTransform. |
